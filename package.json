{
  "name": "yday_spottobe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ui:dev": "pnpm --filter ui start",
    "api:dev": "pnpm --filter api start:dev",
    "api:test": "pnpm --filter api test",
    "api:test:cov": "pnpm --filter api test:cov",
    "api:test:e2e": "pnpm --filter api test:e2e",
    "lint:ui": "pnpm --filter ui lint",
    "lint:api": "pnpm --filter api lint",
    "build:packages": "pnpm --filter @spottobe/dtos build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lint-staged": "^15.2.10"
  },
 "lint-staged": {
  "apps/ui/**/*.{js,ts,jsx,tsx}": [
    "pnpm --filter ui lint"
  ],
  "apps/api/**/*.{js,ts,jsx,tsx}": [
    "pnpm --filter api run lint"
  ]
}

}
